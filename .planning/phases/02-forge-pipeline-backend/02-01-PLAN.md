---
phase: 02-forge-pipeline-backend
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/models/forge.py
  - backend/prompts/detect_category.txt
  - backend/prompts/craft_vibe_coding.txt
  - backend/prompts/craft_brainstorming.txt
  - backend/prompts/craft_qa.txt
  - backend/tests/test_forge.py
autonomous: true
requirements: [PIPE-01, PIPE-02]

must_haves:
  truths:
    - "ForgeRequest rejects input shorter than 3 chars with 422"
    - "ForgeRequest rejects input longer than 1000 chars with 422"
    - "ForgeRequest accepts valid input between 3-1000 chars"
    - "Category detection parses 'vibe_coding' from Granite text"
    - "Category detection parses 'brainstorming' from Granite text"
    - "Category detection parses 'qa' from Granite text"
    - "Category detection defaults to 'brainstorming' on ambiguous input"
  artifacts:
    - path: "backend/models/forge.py"
      provides: "ForgeRequest, ForgeResponse, CallTiming models"
      exports: ["ForgeRequest", "ForgeResponse", "CallTiming"]
    - path: "backend/prompts/detect_category.txt"
      provides: "Category detection prompt template"
      contains: "{input}"
    - path: "backend/prompts/craft_vibe_coding.txt"
      provides: "Vibe coding crafting template"
      contains: "{input}"
    - path: "backend/prompts/craft_brainstorming.txt"
      provides: "Brainstorming crafting template"
      contains: "{input}"
    - path: "backend/prompts/craft_qa.txt"
      provides: "QA crafting template"
      contains: "{input}"
    - path: "backend/tests/test_forge.py"
      provides: "All PIPE-01 through PIPE-07 test cases"
      min_lines: 80
  key_links:
    - from: "backend/models/forge.py"
      to: "backend/tests/test_forge.py"
      via: "test imports ForgeRequest, ForgeResponse, CallTiming"
      pattern: "from backend\\.models\\.forge import"
---

<objective>
Pydantic models, prompt templates, and test scaffold (TDD RED phase) for the forge pipeline.

Purpose: Establish the data contracts (ForgeRequest/ForgeResponse/CallTiming), write all four prompt templates that drive Granite calls, and create the full test suite that will go RED — proving the tests are meaningful before implementation begins in Plan 02.

Output: Models in `backend/models/forge.py`, four prompt templates in `backend/prompts/`, complete test file `backend/tests/test_forge.py` with all PIPE-requirement tests failing (RED).
</objective>

<execution_context>
@/Users/abdullah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-forge-pipeline-backend/02-RESEARCH.md

<interfaces>
<!-- Phase 1 exports consumed by this plan -->

From backend/models/forge.py (EXISTING — extend, do not replace):
```python
from pydantic import BaseModel

class GraniteResponse(BaseModel):
    """Response from a single IBM Granite generate_text call."""
    text: str
    latency_ms: float
```

From backend/services/granite_service.py:
```python
async def generate_text(
    prompt: str,
    call_name: str = "unknown",
    max_tokens: Optional[int] = None,
    temperature: Optional[float] = None,
) -> GraniteResponse:
    ...

class GraniteError(Exception):
    ...
```

From backend/config.py:
```python
class Settings(BaseSettings):
    max_tokens_craft: int = 500
    max_tokens_execute: int = 800
    ...

settings = get_settings()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic models and prompt templates</name>
  <files>backend/models/forge.py, backend/prompts/detect_category.txt, backend/prompts/craft_vibe_coding.txt, backend/prompts/craft_brainstorming.txt, backend/prompts/craft_qa.txt</files>
  <action>
**1. Extend `backend/models/forge.py`** — ADD ForgeRequest, ForgeResponse, and CallTiming below the existing GraniteResponse class. Do NOT remove or modify GraniteResponse.

```python
from pydantic import BaseModel, Field

class ForgeRequest(BaseModel):
    input: str = Field(..., min_length=3, max_length=1000)

class CallTiming(BaseModel):
    call_name: str
    latency_ms: float

class ForgeResponse(BaseModel):
    category: str                  # "vibe_coding" | "brainstorming" | "qa"
    crafted_prompt: str
    crafted_result: str
    raw_result: str
    call_timings: list[CallTiming]
    total_latency_ms: float
```

**2. Write `backend/prompts/detect_category.txt`:**
```
Classify the following user input into exactly one category.
Categories: vibe_coding, brainstorming, qa

Rules:
- vibe_coding: anything about writing code, programming, software, debugging, scripts, APIs
- brainstorming: creative ideas, business plans, strategy, names, concepts, lists of options
- qa: questions seeking factual answers, research, explanations, analysis, comparisons

Output only the category name, nothing else.

Input: {input}
Category:
```

**3. Write `backend/prompts/craft_vibe_coding.txt`:**
```
You are an expert prompt engineer specializing in coding tasks.
Transform the rough idea below into an expert one-shot prompt (200-400 words).

The expert prompt MUST include ALL of these structural elements:
1. ROLE: Define who the AI should act as (e.g., "senior Python developer")
2. CONTEXT: Background information the AI needs to understand the task
3. CONSTRAINTS: Technical requirements, language version, style guides, limits
4. OUTPUT FORMAT: Exact format expected (code block, file structure, comments)
5. QUALITY BAR: What distinguishes excellent output from mediocre output
6. TASK: The specific action to perform, stated clearly and unambiguously

Write the prompt in second person ("You are..."). Be specific and detailed.
Do NOT include any preamble or explanation — output ONLY the expert prompt text.

Rough idea: {input}

Expert prompt:
```

**4. Write `backend/prompts/craft_brainstorming.txt`:**
```
You are an expert prompt engineer specializing in creative and strategic thinking.
Transform the rough idea below into an expert one-shot prompt (200-400 words).

The expert prompt MUST include ALL of these structural elements:
1. ROLE: Define who the AI should act as (e.g., "innovation strategist with 15 years experience")
2. CONTEXT: Background framing that shapes the brainstorming direction
3. CONSTRAINTS: Boundaries like quantity of ideas, feasibility level, target audience
4. OUTPUT FORMAT: How to structure the brainstorm output (numbered list, categories, pros/cons)
5. QUALITY BAR: What makes a brainstorm response genuinely useful vs generic
6. TASK: The specific creative challenge to tackle

Write the prompt in second person ("You are..."). Be specific and detailed.
Do NOT include any preamble or explanation — output ONLY the expert prompt text.

Rough idea: {input}

Expert prompt:
```

**5. Write `backend/prompts/craft_qa.txt`:**
```
You are an expert prompt engineer specializing in research and analytical questions.
Transform the rough idea below into an expert one-shot prompt (200-400 words).

The expert prompt MUST include ALL of these structural elements:
1. ROLE: Define who the AI should act as (e.g., "senior research analyst")
2. CONTEXT: Background knowledge and scope boundaries for the question
3. CONSTRAINTS: Depth of analysis, citation expectations, what to exclude
4. OUTPUT FORMAT: How to structure the answer (sections, headers, evidence format)
5. QUALITY BAR: What distinguishes a thorough answer from a shallow one
6. TASK: The specific question or analysis to perform

Write the prompt in second person ("You are..."). Be specific and detailed.
Do NOT include any preamble or explanation — output ONLY the expert prompt text.

Rough idea: {input}

Expert prompt:
```

All templates use `{input}` as the single format placeholder. No other placeholders.
  </action>
  <verify>
    <automated>cd "/Users/abdullah/projects/Hackathons/GenAI Genesis/PromptForge/promptforge-genesis/backend" && source venv/bin/activate && python -c "from backend.models.forge import ForgeRequest, ForgeResponse, CallTiming, GraniteResponse; print('Models OK')" && python -c "from pathlib import Path; d=Path('prompts'); assert all((d/f).read_text().strip() for f in ['detect_category.txt','craft_vibe_coding.txt','craft_brainstorming.txt','craft_qa.txt']); print('Templates OK')"</automated>
  </verify>
  <done>ForgeRequest, ForgeResponse, CallTiming models importable alongside existing GraniteResponse. All four prompt template files contain non-empty templates with {input} placeholder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test scaffold for all PIPE requirements (RED)</name>
  <files>backend/tests/test_forge.py</files>
  <behavior>
    - test_forge_rejects_short_input: POST /api/forge with {"input":"ab"} returns 422
    - test_forge_rejects_long_input: POST /api/forge with {"input":"x"*1001} returns 422
    - test_forge_accepts_valid_input: POST /api/forge with valid input returns 200 with ForgeResponse shape
    - test_category_detection_vibe_coding: Input about coding is detected as "vibe_coding"
    - test_category_detection_brainstorming: Input about ideas is detected as "brainstorming"
    - test_category_detection_qa: Input about questions is detected as "qa"
    - test_category_detection_default: Ambiguous input defaults to "brainstorming"
    - test_crafted_prompt_populated: ForgeResponse.crafted_prompt is a non-empty string
    - test_parallel_calls_both_present: call_timings contains both "execute_crafted" and "execute_raw"
    - test_execute_calls_are_parallel: With mocked 0.5s delay, total wall time is under 1.2s (not 2x)
    - test_log_event_is_background: BackgroundTasks.add_task is called with the log function
    - test_log_event_failure_is_swallowed: If log function raises, the forge request still returns 200
  </behavior>
  <action>
Write `backend/tests/test_forge.py` with all tests listed above. Tests use `unittest.mock.patch` and `AsyncMock` to mock `backend.services.granite_service.generate_text`.

**Mocking strategy:** Patch `backend.services.forge_service.generate_text` (where it's imported in the service module) not the original module. Each test provides a `side_effect` list of 4 `GraniteResponse` objects matching the 4 calls: detect_category, craft_prompt, execute_crafted, execute_raw.

**Category detection tests (PIPE-02):** These test the `_detect_category_from_text` helper directly by importing it from forge_service. Mock responses return "vibe_coding", "brainstorming", or "qa" as text. Also test with surrounding text like "The category is: brainstorming" to verify substring matching.

**Parallel execution test (PIPE-04):** Mock `generate_text` with `asyncio.sleep(0.5)` delay on calls 3 and 4. Verify total wall time is < 1.2s (proving parallel, not sequential 2x0.5=1.0s).

**BackgroundTasks tests (PIPE-07):**
- test_log_event_is_background: Verify BackgroundTasks.add_task was called
- test_log_event_failure_is_swallowed: Patch `_log_forge_event` to raise Exception, verify POST /api/forge still returns 200

Use `from fastapi.testclient import TestClient` and `from backend.main import app` for route-level tests. Use `client = TestClient(app)` fixture.

These tests MUST FAIL (RED) because forge_service.py and routers/forge.py are empty stubs. Confirm RED status by running them and expecting failures.

IMPORTANT: Import the TestClient inside the test module. Do NOT modify conftest.py from Phase 1.
  </action>
  <verify>
    <automated>cd "/Users/abdullah/projects/Hackathons/GenAI Genesis/PromptForge/promptforge-genesis/backend" && source venv/bin/activate && python -m pytest tests/test_forge.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All 12 tests exist in test_forge.py. Tests fail (RED) because forge_service and forge router are not yet implemented. Phase 1 tests (test_granite.py) still pass.</done>
</task>

</tasks>

<verification>
- `python -c "from backend.models.forge import ForgeRequest, ForgeResponse, CallTiming"` succeeds
- All 4 prompt template files are non-empty and contain `{input}`
- `pytest tests/test_forge.py --collect-only` shows 12 test items
- `pytest tests/test_granite.py -v` still passes (Phase 1 regression)
</verification>

<success_criteria>
- ForgeRequest, ForgeResponse, CallTiming models are importable and have correct field types
- Four prompt templates exist with {input} placeholder and structural element instructions
- 12 tests exist and are in RED state (failing due to missing implementation)
- Phase 1 test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-forge-pipeline-backend/02-01-SUMMARY.md`
</output>
