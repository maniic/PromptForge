---
phase: 04-frontend-shell
plan: 00
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/components/forge/ForgeApp.test.tsx
  - frontend/src/components/forge/ThreeColumnLayout.test.tsx
  - frontend/src/components/shared/LoadingStatus.test.tsx
  - frontend/src/lib/api.test.ts
  - frontend/package.json
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-06
  - UX-07

must_haves:
  truths:
    - "npm run test -- --run executes without configuration errors"
    - "Test stubs exist for all core modules and render without crashing"
  artifacts:
    - path: "frontend/vitest.config.ts"
      provides: "Vitest configuration with jsdom environment and path aliases"
    - path: "frontend/src/test/setup.ts"
      provides: "Testing library jest-dom matchers setup"
    - path: "frontend/src/components/forge/ForgeApp.test.tsx"
      provides: "Stub tests for ForgeApp (UX-01, UX-02)"
    - path: "frontend/src/lib/api.test.ts"
      provides: "Stub tests for API client error formatting (UX-07)"
  key_links:
    - from: "frontend/vitest.config.ts"
      to: "frontend/src/test/setup.ts"
      via: "setupFiles config array"
      pattern: "setupFiles.*setup"
---

<objective>
Create the Vitest test infrastructure and stub test files for all core frontend modules. This is Wave 0 — test scaffolding that all subsequent plans' verify steps depend on.

Purpose: Enables `npm run test -- --run` as a verification command across all phase 4 plans, satisfying Nyquist sampling requirements.
Output: Working vitest config, setup file, and stub test files for ForgeApp, ThreeColumnLayout, LoadingStatus, and api client.
</objective>

<execution_context>
@/Users/abdullah/.claude/get-shit-done/workflows/execute-plan.md
@/Users/abdullah/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-shell/04-CONTEXT.md
@.planning/phases/04-frontend-shell/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vitest config, setup, and test script</name>
  <files>
    frontend/vitest.config.ts
    frontend/src/test/setup.ts
    frontend/package.json
  </files>
  <action>
    1. **frontend/vitest.config.ts** — Create Vitest configuration:
       ```typescript
       import { defineConfig } from 'vitest/config'
       import react from '@vitejs/plugin-react'
       import path from 'path'

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: 'jsdom',
           setupFiles: ['./src/test/setup.ts'],
           globals: true,
         },
         resolve: {
           alias: {
             '@': path.resolve(__dirname, './src'),
           },
         },
       })
       ```

    2. **frontend/src/test/setup.ts** — Import jest-dom matchers:
       ```typescript
       import '@testing-library/jest-dom/vitest'
       ```

    3. **frontend/package.json** — Add test script if not present:
       - Add `"test": "vitest"` to scripts section

    4. Verify config works: `cd frontend && npm run test -- --run` (should pass with 0 tests found, no config errors).

    NOTE: vitest, @vitejs/plugin-react, jsdom, @testing-library/react, @testing-library/jest-dom, and @testing-library/user-event are already installed by 04-01 Task 1 step 6.
  </action>
  <verify>
    <automated>cd frontend && npm run test -- --run 2>&1 | tail -5</automated>
  </verify>
  <done>
    - vitest.config.ts exists with jsdom environment, react plugin, and @ alias
    - src/test/setup.ts imports jest-dom matchers
    - `npm run test -- --run` executes without configuration errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Stub test files for all core modules</name>
  <files>
    frontend/src/components/forge/ForgeApp.test.tsx
    frontend/src/components/forge/ThreeColumnLayout.test.tsx
    frontend/src/components/shared/LoadingStatus.test.tsx
    frontend/src/lib/api.test.ts
  </files>
  <action>
    Create stub test files that will initially test basic rendering/existence. These stubs will be expanded by later plans as components are built. Each stub should import the module under test and have at least one meaningful assertion.

    1. **frontend/src/lib/api.test.ts** — Tests for the API client (built by 04-01):
       - Test: `formatApiError` returns correct message for 422 status
       - Test: `formatApiError` returns correct message for 502 status
       - Test: `formatApiError` handles generic Error with message
       - Test: `formatApiError` handles unknown error type
       - Import `formatApiError` from `@/lib/api`

    2. **frontend/src/components/forge/ForgeApp.test.tsx** — Stub for ForgeApp (built by 04-02):
       - Test: `ForgeApp renders without crashing` — render `<ForgeApp />`, expect the document body to contain rendered content
       - Test: `ForgeApp shows hero input in idle state` — render, check for presence of textarea or "Forge" button text
       - Mock `@/lib/api` module to avoid real API calls
       - Wrap renders in necessary providers if needed (but ForgeApp should be self-contained)

    3. **frontend/src/components/forge/ThreeColumnLayout.test.tsx** — Stub for ThreeColumnLayout (built by 04-03):
       - Test: `ThreeColumnLayout renders all three columns` — render with mock ForgeResponse data, check for "Your Input", "Crafted Prompt", and result sections
       - Create a `mockForgeResponse` fixture: `{ category: 'vibe_coding', crafted_prompt: 'Test prompt', crafted_result: 'Test crafted result', raw_result: 'Test raw result', call_timings: [], total_latency_ms: 1000 }`

    4. **frontend/src/components/shared/LoadingStatus.test.tsx** — Stub for LoadingStatus (built by 04-02):
       - Test: `LoadingStatus renders nothing when not loading` — render with `isLoading={false}`, expect no status text
       - Test: `LoadingStatus shows first stage when loading` — render with `isLoading={true}`, expect "Detecting category..." text

    IMPORTANT: ForgeApp.test.tsx and ThreeColumnLayout.test.tsx will initially fail (components don't exist yet). That's expected — they become green as 04-02 and 04-03 execute. The api.test.ts and LoadingStatus.test.tsx tests should pass after their respective plan tasks complete.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5; echo "---"; npm run test -- --run src/lib/api.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>
    - 4 test files exist at the paths specified in VALIDATION.md
    - api.test.ts passes (tests formatApiError which exists from 04-01)
    - ForgeApp.test.tsx, ThreeColumnLayout.test.tsx, LoadingStatus.test.tsx exist as stubs (may fail until their components are built)
    - All test files compile without TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `cd frontend && npm run test -- --run` executes without vitest configuration errors
- api.test.ts tests pass against the formatApiError function from 04-01
- All 4 test stub files exist at paths matching VALIDATION.md Wave 0 Requirements
</verification>

<success_criteria>
- Vitest configured with jsdom, react plugin, and path aliases
- `npm run test -- --run` works as a verification command
- Test stubs exist for ForgeApp, ThreeColumnLayout, LoadingStatus, and api
- api.test.ts passes with meaningful assertions on formatApiError
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-shell/04-00-SUMMARY.md`
</output>
