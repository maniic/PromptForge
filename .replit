run = "cd backend && pip install -r requirements.txt && uvicorn backend.main:app --host 0.0.0.0 --port 8000"
language = "python3"

[env]
PYTHONPATH = "/home/runner/${REPL_SLUG}"

[nix]
channel = "stable-24_05"
